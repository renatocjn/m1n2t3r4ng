
if (document.timer) {
    document.timer.cancel();
}

var repeating_refresh = function() {
    $('#refresh_form').submit();
    document.timer = new Timer(repeating_refresh, <%=session[:refresh_ratio] * 1000%>);
}

document.timer = new Timer(repeating_refresh, <%=session[:refresh_ratio] * 1000%>);
